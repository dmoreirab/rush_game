const CommonObjects = function ()  {
    this.appURL = 'https://buzz-dev.mindera.com/rush/game';

    /* Future common data-tests
    this.playersTab = cy.get('[data-test=playersTab]');
    this.historyTab = cy.get('[data-test=historyTab]');
    this.betTab = cy.get('[data-test=betTab]');
    this.busted = cy.get('[data-test=busted]');
    this.winMultiplier = cy.get('[data-test=winMultiplier]');
    this.nextRound-timer = cy.get('[data-test=nextRound-timer]');
    this.placeBet-timer = cy.get('[data-test=placeBet-timer]');
    this.menu = cy.get('[data-test=menu]');
    */
};

module.exports = CommonObjects;